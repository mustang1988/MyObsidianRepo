```dataviewjs
const fishes = dv.pages('#英雄传说/闪之轨迹/笔记/钓鱼 and -"模板"');
const rods = await Promise.all(fishes.map(f => 
  dv.view('Item', { items: f.Rods, options: { inline: false, raw: true } })
));

dv.table(
   ["", "名称", "鱼竿", "位置"],
   fishes.map((fish, index) => [
    dv.fileLink(fish.Icon.path, true, '50'),
    fish.file.link,
    rods[index],
    fish.Positions
  ])
);
```

#英雄传说/闪之轨迹/笔记/汇总   