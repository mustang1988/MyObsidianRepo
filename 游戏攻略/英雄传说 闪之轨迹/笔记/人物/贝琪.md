---
Name: "贝琪"
FullName: "贝琪"
Category: "1年级(III IV V班)"
CategoryNo: 3
NoInCategory: 13
Description:
  - "1年V班【未加入社团】"
  - "散发商人气质得女学生."
  - "凡是总是以赚钱为优先考量."
Notes:
  - Title: "摆摊对决的结果"
    Content:
      - "在学院祭时与雨果比赛营业额,"
      - "因此互相认同了彼此做生意得能力."
    Time: "[[游戏攻略/英雄传说 闪之轨迹/攻略/终章 士官学院祭、然后——#^ch-fin-10-24-character-01|终章10/24]]"
    How: "主校舍前和贝琪对话"
---
```dataviewjs
const { FullName, Name: name, Description: desc, Notes: notes } = dv.current();
const category = 'ColdSteel'
const icon = await dv.view('Icons/Character', { name, category, options: { raw: true } });
const iconHTML = await dv.view('Icons/GetIconHTML', { icon, options: { size: 100, raw: true } });
// Output
dv.header(1, FullName);
dv.table(
  [],
  [
    [
      iconHTML,
      desc
    ],
  ]
);
dv.span('---')
```
```dataviewjs
const { Notes } = dv.current();
const [ note, , ] = Notes;
if(note) {
  const { Title, Content, Time, How } = note;
    const timeLink = dv.blockLink(Time.path, Time.subpath, false, Title);
  dv.table(
    [],
    [
      [
        timeLink,
        Content,
      ],
    ]
  );
  dv.span('---')
}
```
^001

```dataviewjs
const { Notes } = dv.current();
const [ ,note, ] = Notes;
if(note) {
  const { Title, Content, Time, How } = note;
    const timeLink = dv.blockLink(Time.path, Time.subpath, false, Title);
  dv.table(
    [],
    [
      [
        timeLink,
        Content,
      ],
    ]
  );
  dv.span('---')
}
```
^002

```dataviewjs
const { Notes } = dv.current();
const [ , ,note ] = Notes;
if(note) {
  const { Title, Content, Time, How } = note;
    const timeLink = dv.blockLink(Time.path, Time.subpath, false, Title);
  dv.table(
    [],
    [
      [
        timeLink,
        Content,
      ],
    ]
  );
  dv.span('---')
}
```
^003

#英雄传说/闪之轨迹/笔记/人物 