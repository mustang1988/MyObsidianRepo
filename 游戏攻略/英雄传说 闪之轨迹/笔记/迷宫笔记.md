```dataviewjs
const locations = dv.pages('#英雄传说/闪之轨迹/笔记/迷宫 and -"模板"').sort(location => {
  const chapter = location.Chapter[0];
  const chapterPath = chapter.path;
  const chapterSubPath = chapter.subpath
  const chapterPage = dv.page(chapterPath);
  const {No} = chapterPage;
  return [No, chapterSubPath]
}, 'asc');
dv.table(
  ["", "章节"],
  locations.map(l => [
    l.file.link,
    l.Chapter,
  ])
);
```

#英雄传说/闪之轨迹/笔记/汇总   