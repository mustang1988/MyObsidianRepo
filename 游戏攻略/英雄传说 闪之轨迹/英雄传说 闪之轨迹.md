---
banner: "![[图片/游戏攻略/英雄传说/英雄传说 闪之轨迹/cover.jpg]]"
banner_y: 0.32731
Icon: "![[图片/游戏攻略/英雄传说/英雄传说 闪之轨迹/icon.png|50]]"
No: 6
NPWR: "NPWR04672_00"
Developers: 
  - "Falcom"
Publishers: 
  - "Falcom"
  - "云豹娱乐"
  - "SIE"
Producers:  
Release: "2018/03/08"
Abstract: "军事大国《埃雷波尼亚帝国》, 因《改革派》与《贵族派》的对立而政局紧绷. 被遴选进入名门《托尔兹军官学院》新设立《特科班VII班》的新生黎恩, 将与其他《VII班》成员一同迎接波涛汹涌的学院生活!"
Prev: 
Next: "[[游戏攻略/英雄传说 闪之轨迹 II/英雄传说 闪之轨迹II]]"
---
# 英雄传说 闪之轨迹
```ad-quote
title: 奖杯
icon: trophy
collapse: close
~~~dataviewjs
const order = ["白金","金", "银", "铜"]
const trophies = dv.pages('#英雄传说/闪之轨迹/奖杯 and -"模板"')
	.map(trophy => {
		const [tTag] = trophy.file.etags.filter(tag => tag.startsWith('#奖杯') && tag .includes("/"));
		const [, type] = tTag.split('/')
		trophy.Type = type;
		return trophy;
	})
	.map(trophy => {
		trophy.Cp = trophy.Chapter ? dv.page(trophy.Chapter): {};
		return trophy;
	})
	.sort(trophy => {
		return [order.indexOf(trophy.Type), trophy.Cp.No, trophy.Cp?.path, trophy.Cp?.subpath]
	}, 'asc')
	.map(trophy => [
			dv.fileLink(trophy.Icon.path, true, "30"), 
			trophy.file.link, 
			trophy.Comment,
			trophy.Chapter
		]);
dv.table(
	[],
	trophies
);
~~~
```

```ad-quote
title: 攻略
icon: list
collapse: close
~~~dataviewjs
dv.table(
	[""], 
	dv.pages('#英雄传说/闪之轨迹/攻略')
		.sort(page => [page.No], 'asc')
		.map(page => [page.file.link])
);
~~~
```

````ad-quote
title: 笔记
icon: pen
collapse: close

```ad-quote
title: 钓鱼
icon: fish
collapse: close
~~~dataviewjs
const fish_notes = dv.pages('"游戏攻略/英雄传说 闪之轨迹/笔记/钓鱼"')
                  .map(fish=>[
              	   fish.file.link,
              	   fish.Rods,
              	   fish.Positions,
              	   fish.Bonus.map(b => dv.paragraph(
                                                	   (Array.isArray(b.Icon) ? b.Icon.join('') : b.Icon) 
                                                	   + b.Name 
                                                	   + (b.Num > 1 ? 'x' + b.Num : ''))
                                                	  )
              	 ]);
dv.table(
   ["","鱼竿","位置","掉落"],
   fish_notes
);
~~~
```

```ad-quote
title: 书籍
icon: book
collapse: close
~~~dataviewjs
const books = dv.pages('"游戏攻略/英雄传说 闪之轨迹/笔记/书籍"')
              .sort(book => book.TimeToGet.subpath, 'asc')
              .map(book => [
                book.file.link,
                book.TimeToGet,
                book.WayToGet,
                book.Series
              ]);
dv.table(
   ["", "获取章节", "获取方式", "分类"],
   books
 );
~~~
```

```ad-quote
title: 食谱
icon: receipt
collapse: close
~~~dataviewjs
const recipes = dv.pages('#英雄传说/闪之轨迹/笔记/食谱 and -"模板"')
      .sort(recipe => recipe.TimeToGet.subpath, 'asc')
      .map(recipe => [
  		 recipe.file.link,
  		 recipe.TimeToGet, 
  		 recipe.WayToGet, 
  		 recipe.Special.Cook[0],
  		 [
    		 dv.paragraph(dv.fileLink(recipe.Best.Icon.path, true, "20") + recipe.Best.Name),
    		 dv.paragraph(dv.fileLink(recipe.Normal.Icon.path, true, "20") + recipe.Normal.Name),
    		 dv.paragraph(dv.fileLink(recipe.Strange.Icon.path, true, "20") + recipe.Strange.Name),
    		 dv.paragraph(dv.fileLink(recipe.Special.Icon.path, true, "20") + recipe.Special.Name),
    	 ]
  	  ]);
dv.table(
   ["", "获取章节", "获取方式", "独门料理人", "极品/普通/古怪/独门"],
   recipes
);
~~~
```

```ad-quote
title: 核心回路
icon: crystals
collapse: close
~~~dataviewjs
const element_order = ["地", "水", "火", "风", "时", "空", "幻"]
const master_quartz = dv.pages('#英雄传说/闪之轨迹/笔记/核心回路  and -"模板"')
                     .sort(quartz => element_order.indexOf(quartz.Element), "asc")
                     .map(quartz => [
                        quartz.Icon,
                        quartz.file.link,
                        quartz.TimeToGet,
                        quartz.WayToGet
                     ])
dv.table(
   ["", "名称", "获取章节", "获取方式"],
   master_quartz
);
~~~
```

```ad-quote
title: 任务笔记
icon: question
collapse: close
~~~dataviewjs
const quests = dv.pages('#英雄传说/闪之轨迹/笔记/任务 and -"模板"')
               .map(quest => {
                  quest.ChapterObj = dv.page(quest.Time.path);
                  return quest;
               })
               .sort(quest => [quest.ChapterObj.No, quest.Time.subpath], 'asc')
               .map(quest => [
                  quest.file.link, 
                  quest.Type, 
                  quest.ExtraAP == 0 
                     ? quest.BasicAP
                     : `${quest.BasicAP} + ${quest.ExtraAP}`,
                  quest.Time, 
                  quest.Steps, 
                  quest.Bonus.map(
                          b => dv.paragraph(typeof b == 'string' 
                                            ? b 
                                            : (Array.isArray(b.Icon) 
                                                ? b.Icon.join('') 
                                                : b.Icon) 
                                              + b.Name + (b.Num > 1 
                                                ? 'x'+b.Num
                                                : '')
                                              )
                          )
               ]);
dv.table(
   ["", "类型", "AP", "章节", "步骤", "奖励"],
   quests
);
~~~
```

```ad-quote
title: 人物信息
icon: people
collapse: close
~~~dataviewjs
const datas = dv.pages('#英雄传说/闪之轨迹/笔记/人物 and -"模板"')
              .sort(page => {
                return [dv.page(page.TimeToGet.path).No, page.TimeToGet.subpath];
              },'asc')
              .map(page => [
                page.file.link,
                page.Icon ? dv.paragraph(dv.fileLink(page.Icon.path, true, '30')) : page.Character,
                page.TimeToGet,
                page.WayToGet
              ]);
dv.table(
  ["", "人物", "章节", "获取方式"],
  datas
);
~~~
```

```ad-quote
title: 战斗笔记
icon: crossed-swords
collapse: close
~~~dataviewjs
const datas = dv.pages('#英雄传说/闪之轨迹/笔记/战斗 and -"模板"')
              .sort(page => {
                const position = dv.page(page.Positions[0].path);
                const chapter = dv.page(position.Chapter.path);
                return [chapter.No, position.Name, page.file.name];
              }, 'asc')
              .map(item => [
                 item.file.link,
                 item.Positions,
                 item.Bonus ? item.Bonus.map(b => dv.paragraph((Array.isArray(b.Icon) ? b.Icon.join('') : b.Icon) + b.Name + (b.Num > 1 ? 'x'+b.Num: ''))) : '无'
               ]);
dv.table(
   ["", "地点", "掉落"],
   datas
);
~~~
```

```ad-quote
title: 迷宫
icon: map-marked
collapse: close
~~~dataviewjs
const locations = dv.pages('#英雄传说/闪之轨迹/笔记/迷宫 and -"模板"')
                 .map(local => {
                    local.ChapterObj = dv.page(local.Chapter.path);
                    return local;
                 })
                 .sort(local => [local.ChapterObj.No, local.Chapter.subpath], 'asc')
                 .map(local => [
                   local.file.link,
                   local.Chapter
                 ]);
dv.table(
   ["", "章节"],
   locations
);
~~~
```

```ad-quote
title: 宝箱
icon: box
collapse: close
~~~dataviewjs
dv.table(
   ["", "奖励", "章节", "地点"],
   dv.pages('#英雄传说/闪之轨迹/笔记/宝箱 and -"模板"')
   .sort(box => [parseFloat(box.file.name)], 'asc')
   .map(box => [
     box.file.link,
     dv.paragraph(box.Bonus.path 
            ? box.Bonus 
            : (Array.isArray(box.Bonus.Icon) 
              ? box.Bonus.Icon.join('') 
              : box.Bonus.Icon) 
            + box.Bonus.Name 
            + (box.Bonus.Num > 1 
              ? 'x'+box.Bonus.Num 
              : '')
            ),
     box.Chapter,
     box.Position
   ])
);
~~~
```

````

#英雄传说/闪之轨迹 #角色扮演 #回合制 #系列/英雄传说 #游戏首页 #日式RPG