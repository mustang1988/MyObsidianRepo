---
List:
  - Name: "琥曜石"
    ID: "sepithearth"
    Element: "Earth" 
  - Name: "水曜石"
    ID: "sepithwater"
    Element: "Water"
  - Name: "火曜石"
    ID: "sepithfire"
    Element: "Fire"
  - Name: "风曜石"
    ID: "sepithwind"
    Element: "Wind"
  - Name: "时曜石"
    ID: "sepithtime"
    Element: "Time"
  - Name: "空曜石"
    ID: "sepithspace"
    Element: "Space"
  - Name: "幻曜石"
    ID: "sepithmirage"
    Element: "Mirage"
  - Name: "耀晶块"
    ID: "sepithmass"
    Element: "Mass"
  - Name: "七曜石"
    ID: "sepithall"
    Element: "All"
  - Name: "U物质"
    ID: "umaterial"
---
```dataviewjs
dv.span(await dv.view('Item/Sepith/Admonition', { sepith: dv.current().List[0], options: { raw: true } }));
```
^sepithearth

```dataviewjs
dv.span(await dv.view('Item/Sepith/Admonition', { sepith: dv.current().List[1], options: { raw: true } }));
```
^sepithwater

```dataviewjs
dv.span(await dv.view('Item/Sepith/Admonition', { sepith: dv.current().List[2], options: { raw: true } }));
```
^sepithfire

```dataviewjs
dv.span(await dv.view('Item/Sepith/Admonition', { sepith: dv.current().List[3], options: { raw: true } }));
```
^sepithwind

```dataviewjs
dv.span(await dv.view('Item/Sepith/Admonition', { sepith: dv.current().List[4], options: { raw: true } }));
```
^sepithtime

```dataviewjs
dv.span(await dv.view('Item/Sepith/Admonition', { sepith: dv.current().List[5], options: { raw: true } }));
```
^sepithspace

```dataviewjs
dv.span(await dv.view('Item/Sepith/Admonition', { sepith: dv.current().List[6], options: { raw: true } }));
```
^sepithmirage

```dataviewjs
dv.span(await dv.view('Item/Sepith/Admonition', { sepith: dv.current().List[7], options: { raw: true } }));
```
^sepithmass

```dataviewjs
dv.span(await dv.view('Item/Sepith/Admonition', { sepith: dv.current().List[8], options: { raw: true } }));
```
^sepithall

```dataviewjs
dv.span(await dv.view('Item/UMaterial/Admonition', { item: dv.current().List[9], options: { raw: true } }));
```
^umaterial

---
```dataviewjs
dv.header(2,"行内渲染")

dv.header(3,"耀晶片不带名称行内渲染")
// 耀晶片不带名称行内渲染
dv.list(await Promise.all(dv.current().List.filter(i => i.Element).map(i => dv.blockLink(dv.current().file.path, i.ID)).map(i => dv.view('Item/Sepith/InLine', { link: i, options: { raw: true } }))));

dv.header(3,"耀晶片带名称行内渲染")
// 耀晶片带名称行内渲染
dv.list(await Promise.all(dv.current().List.filter(i => i.Element).map(i => dv.blockLink(dv.current().file.path, i.ID)).map(i => dv.view('Item/Sepith/InLine', { link: i, options: { raw: true, display_name: true } }))));

dv.header(3,"U物质不带名称行内渲染")
// U物质不带名称行内渲染
dv.list(await Promise.all(dv.current().List.filter(i => i.ID === "umaterial").map(i => dv.blockLink(dv.current().file.path, i.ID)).map(i => dv.view('Item/UMaterial/InLine', { link: i, options: { raw: true } }))));

dv.header(3,"U物质带名称行内渲染")
// U物质带名称行内渲染
dv.list(await Promise.all(dv.current().List.filter(i => i.ID === "umaterial").map(i => dv.blockLink(dv.current().file.path, i.ID)).map(i => dv.view('Item/UMaterial/InLine', { link: i, options: { raw: true, display_name: true } }))));

dv.header(3,"\"通用\"耀晶片不带名称行内渲染")
dv.list(await Promise.all(dv.current().List.filter(i => i.Element).map(i => dv.blockLink(dv.current().file.path, i.ID)).map(i => dv.view('Item/InLine', { link: i, options: { raw: true, type: "Item.Sepith.InLine" } }))));

dv.header(3,"\"通用\"耀晶片带名称行内渲染")
dv.list(await Promise.all(dv.current().List.filter(i => i.Element).map(i => dv.blockLink(dv.current().file.path, i.ID)).map(i => dv.view('Item/InLine', { link: i, options: { raw: true, type: "Item.Sepith.InLine", display_name: true } }))));

dv.header(3,"\"通用\"U物质不带名称行内渲染")
dv.list(await Promise.all(dv.current().List.filter(i => i.ID === "umaterial").map(i => dv.blockLink(dv.current().file.path, i.ID)).map(i => dv.view('Item/InLine', { link: i, options: { raw: true, type: "Item.UMaterial.InLine" } }))));

dv.header(3,"\"通用\"U物质带名称行内渲染")
dv.list(await Promise.all(dv.current().List.filter(i => i.ID === "umaterial").map(i => dv.blockLink(dv.current().file.path, i.ID)).map(i => dv.view('Item/InLine', { link: i, options: { raw: true, display_name: true, type: "Item.UMaterial.InLine" } }))));
```

---


```dataviewjs
dv.header(2,"带数量行内渲染")

dv.header(3,"耀晶片不带名称带数量渲染")
// 耀晶片不带名称带数量渲染
dv.list(await Promise.all(dv.current().List.filter(i => i.Element).map(i => dv.blockLink(dv.current().file.path, i.ID)).map(i => dv.view('Item/Sepith/Count', { link: i, options: { raw: true, count: 100, limit: 1 } }))));

dv.header(3,"耀晶片带名称带数量渲染")
// 耀晶片带名称带数量渲染
dv.list(await Promise.all(dv.current().List.filter(i => i.Element).map(i => dv.blockLink(dv.current().file.path, i.ID)).map(i => dv.view('Item/Sepith/Count', { link: i, options: { raw: true, count: 100, limit: 1, display_name: true } }))));


dv.header(3,"U物质不带名称数量行内渲染")
// U物质不带名称数量行内渲染
dv.list(await Promise.all(dv.current().List.filter(i => i.ID === "umaterial").map(i => dv.blockLink(dv.current().file.path, i.ID)).map(i => dv.view('Item/UMaterial/Count', { link: i, options: { raw: true, count: 10, limit: 1 } }))));

dv.header(3,"U物质带名称数量行内渲染")
// U物质带名称数量行内渲染
dv.list(await Promise.all(dv.current().List.filter(i => i.ID === "umaterial").map(i => dv.blockLink(dv.current().file.path, i.ID)).map(i => dv.view('Item/UMaterial/Count', { link: i, options: { raw: true, count: 10, display_name: true, limit: 1 } }))));

```
