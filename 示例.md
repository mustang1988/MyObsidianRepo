# 图标
## BUFF
```dataviewjs
const buffs = [
"STR+",
"STR++",
"STR+++",
"DEF+",
"DEF++",
"DEF+++",
"ATS+",
"ATS++",
"ATS+++",
"ADF+",
"ADF++",
"ADF+++",
"SPD+",
"SPD++",
"SPD+++",
"MOV+",
"MOV++",
"MOV+++",
"心眼",
"觉醒",
"HP回复",
"CP回复",
"隐身",
"完全防御",
"物理免疫",
"反弹",
"魔法反弹",
"物理反弹",
];
for(const name of buffs){
  const icon = await dv.view('Icons/Buff/Icons', { name, options: { raw: true } });
  const html = await dv.view('Icons/GetIconHTML', { icon, options: { raw: true, size: 40 } });
  dv.span(html)
}
```
## DEBUFF
```dataviewjs
const debuffs = [
"STR-",
"STR--",
"STR---",
"DEF-",
"DEF--",
"DEF---",
"ATS-",
"ATS--",
"ATS---",
"ADF-",
"ADF--",
"ADF---",
"SPD-",
"SPD--",
"SPD---",
"MOV-",
"MOV--",
"MOV---",
"中毒",
"炎伤",
"延迟",
"虚弱",
"消失",
"睡眠",
"石化",
"气绝",
"能力降低",
"魅惑",
"即死",
"混乱",
"黑暗",
"封魔",
"封技",
"噩梦",
"断开连接",
"冻结",
"失衡",
];
for(const name of debuffs){
  const icon = await dv.view('Icons/Debuff/Icons', { name, options: { raw: true } });
  const html = await dv.view('Icons/GetIconHTML', { icon, options: { raw: true, size: 40 } });
  dv.span(html)
}
```
## 元素
```dataviewjs
const elements = ['地', '水', '火', '风', '时', '空', '幻', '阳', '月', '星', '龙'];
for(const name of elements){
  const icon = await dv.view('Icons/Element/Icons', { name, options: { raw: true } });
  const html = await dv.view('Icons/GetIconHTML', { icon, options: { raw: true, size: 40 } });
  dv.span(html)
}
```
## 物品
## 回路
## AT奖励
## 其他